import Navbar from '../components/Navbar';

const Inicio = () => {
  return (
    <div>
      <Navbar />
      {/* Aquí irá el contenido principal del feed, calendario, etc */}
    </div>
  );
};

export default Inicio;
